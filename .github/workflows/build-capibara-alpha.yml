name: Capibara staging

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - name: Staging production overlay
        run: echo "Preparing the 12-iteration Capibara Beta pipeline"
